
'use server';

import { httpsCallable } from 'firebase/functions';
import { auth, functions } from '@/lib/firebase';
import type { Delivery } from './deliveries';

// Este arquivo agora está vazio, pois a lógica foi movida para
// o componente do lado do cliente para resolver problemas de autenticação.
// A função exportada foi removida para evitar o uso incorreto.
