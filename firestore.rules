rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // REGRAS SIMPLES - Qualquer usuário autenticado pode ler/gravar qualquer documento
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}